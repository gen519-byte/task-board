# Task Board Project

## プロジェクト概要

タスク管理ボードアプリケーション。

## Git 運用ルール

### 基本方針
- **コードを変更するたびに必ず GitHub へプッシュする**
- `main` ブランチへの直接プッシュは禁止。作業は必ずフィーチャーブランチで行う
- コミットメッセージは日本語または英語で簡潔に記述する

### ブランチ命名規則
- 機能追加: `feature/<機能名>`
- バグ修正: `fix/<バグ概要>`
- リファクタリング: `refactor/<対象>`

### コミット & プッシュ手順
1. 変更をステージング: `git add <対象ファイル>`
2. コミット: `git commit -m "<変更内容>"`
3. プッシュ: `git push origin <ブランチ名>`

変更のたびにこの手順を実行し、ローカルのみに変更を留めない。

### PR ルール
- フィーチャーブランチの作業完了後、`main` へ Pull Request を作成する
- PR タイトルは変更内容を端的に表す

## コーディング規則

- コメントは原則不要。変数名・関数名で意図を表現する
- 不要な抽象化・将来の拡張を見越した設計は行わない
- セキュリティ上の脆弱性（XSS、SQL インジェクション等）を絶対に混入させない

## 開発環境

- OS: macOS
- Shell: zsh

## GitHub リポジトリ

https://github.com/gen519-byte/task-board
